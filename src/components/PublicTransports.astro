---
const {
  text,
  color,
  isWhite = false,
} = Astro.props as {
  text: string;
  color: string;
  isWhite: boolean;
};
---

<span
  style={"background-color:" + color}
  class={"mx-1px rounded px-2 font-semibold " +
    (isWhite ? "text-white" : "text-zinc-950")}
  >{text}</span
>
