---
interface Props {
  breadcrumbs?: Array<{
    name: string;
    path: string;
  }>;
}
import Logo from "./Logo.astro";
import Sidebar from "./Sidebar.astro";
import ThemeToggle from "./ThemeToggle.astro";

const { breadcrumbs = [] } = Astro.props;
---

<header>
  <nav class="mb-5 flex h-20 w-screen items-center gap-4 px-6">
    <div class="flex flex-1 items-center gap-3">
      <a
        href="/"
        aria-label="Home page"
        class="hover:scale-105 motion-safe:transition-transform motion-safe:duration-200">
        <Logo />
      </a>
      {
        breadcrumbs.map((breadcrumb) => (
          <>
            <span>/</span>
            <a
              class="font-semibold"
              href={breadcrumb.path}>
              {breadcrumb.name}
            </a>
          </>
        ))
      }
    </div>
    <div class="mr-4 hidden justify-center gap-6 md:visible md:flex">
      <a
        href="/projects"
        class="font-semibold"
        >Projects</a
      >
      <a
        href="/blog"
        class="font-semibold"
        >Blog</a
      >
    </div>
    <ThemeToggle />
    <div class="flex justify-center gap-4 md:hidden">
      <Sidebar />
    </div>
  </nav>
</header>
